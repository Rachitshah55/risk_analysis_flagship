### Environment Pins
- Python: 3.13.x (dev), 3.11.x (CI runners)
- Dependencies pinned in `requirements.lock` (generated via `pip freeze`)
- Update policy: bump intentionally; regenerate lock and test CI.
