# --- Core numeric pins to avoid source builds on Python 3.13 ---
numpy>=2.1,<3            # Wheels available for Py3.13
scipy>=1.14              # Modern wheels; plays nice with NumPy 2.x

# --- Your original deps (kept) ---
pandas
scikit-learn
xgboost
lightgbm            # If this fails on Windows, comment it out for now
shap
great-expectations  # GE will be wired fully in a later stage
evidently
mlflow
dvc
dvc-s3              # Optional; keep even if you don’t use S3 yet
prefect
fastapi
uvicorn
python-dotenv
